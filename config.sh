# Just a separate file to hold some config values and source elsewhere 

# All deb/apt based parent images to build from
deb_parents=(ubuntu:22.04 debian:buster-slim)

# Use the same version of zinit throughout
zinit_version=0.2.14